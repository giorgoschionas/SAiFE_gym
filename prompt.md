So, I want to modify the logic of saife to follow mbt in the following sense. Each step_size will be the discretization of the continuous time infinitesimal dt. Hence, we will approximate the order arrivals at  @../SAiFE_gym/SAiFE_gym/stochastic_processes/arrival_models.py  as a Bernoulli trial as is done in mbt at  @../mbt_gym/mbt_gym/stochastic_processes/arrival_models.py. Essentially, we will model our problem as a continuous rebalancing problem. So, at each step, which corresponds to the the discretization of the continuous time infinitesimal dt, we will have at most one buy and one sell order as is it happens in mbt. Since, the order of execution matters in AMM with CL for the LP's earnings, we will flip a random coin to specify which order is processed first so as not create directional bias. 